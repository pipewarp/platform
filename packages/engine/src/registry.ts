// import { StepExecutor } from "@pipewarp/core/types";
// import { warpExecutor } from "@pipewarp/core/executors";

// export class StepRegistry {
//   #executors = new Map<string, StepExecutor>();

//   constructor() {}

//   add(stepType: string, executor: StepExecutor) {
//     this.#executors.set(stepType, executor);
//   }
//   get(stepType: string) {
//     return this.#executors.get(stepType);
//   }
// }

// export const stepRegistry = new StepRegistry();
// stepRegistry.add("warp", warpExecutor);
